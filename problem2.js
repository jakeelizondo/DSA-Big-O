function isEven(value) {
  if (value % 2 === 0) {
    return true;
  } else {
    return false;
  }
}

// O(1) because it is an arithmetic operation. Only one or two operations is performed every time regardless of input size
